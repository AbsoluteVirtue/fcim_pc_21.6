/*
Задание: Определить частоту повторения каждой буквы в заданной строке, 
не изменяя саму строку (без пробелов, максимальная длина - 50 символов, 
заглавные буквы считать отдельно).

Пример ввода: abracadabra
Пример вывода: a 5 b 2 c 1 d 1 r 2
*/
#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char source [50] = {0};

    scanf("%s", source);
    
    // алгоритм и вывод результата здесь

    return 0;
}
